# Öffentliche URL deiner n8n-Instanz (wichtig für Webhooks und OAuth2!)
WEBHOOK_URL=https://n8n.unit-y-ai.io/

# Hostname (z. B. bei Reverse Proxy relevant)
N8N_HOST=n8n.unit-y-ai.io

# Protokoll (https für Produktion)
N8N_PROTOCOL=https

# Port intern im Container (meist 5678)
N8N_PORT=5678

# Gibt an, ob die Editor-UI öffentlich zugänglich ist (true = ja)
N8N_EDITOR_BASE_URL=https://n8n.unit-y-ai.io

# Optional: erlaubt Read-Only-UI (nur Viewer-Modus)
# N8N_READ_ONLY=true

# Deaktiviert das Tracking (DSGVO-konform)
N8N_DISABLE_PRODUCTION_MAIN_MENU_METRICS=true

# Optional: Version Tag für Info
# N8N_VERSION_NOTIFICATIONS_ENABLED=false

# Optional: Setzt die maximale Größe für ausgehende Requests
# N8N_PAYLOAD_SIZE_MAX=16
