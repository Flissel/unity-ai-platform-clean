N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
# Starkes Passwort via Docker Secret (Best Practice)
N8N_BASIC_AUTH_PASSWORD_FILE=/run/secrets/n8n_pw
N8N_COOKIE_SAME_SITE=lax

# JWT vorbereiten für API-Calls (n8n v1.25+)
# N8N_JWT_AUTH_ACTIVE=true
# N8N_JWT_AUTH_HEADER=authorization

# 2FA erst Phase D (Mandanten)
# N8N_2FA_REQUIRED=false
